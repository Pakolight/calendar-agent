# Email address to filter unread Gmail messages
SENDER_EMAIL=organizer@example.com

# OpenAI credentials and model selection
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini

# Optional polling interval in seconds for the bot loop
POLL_INTERVAL_SECONDS=300

# Optional path to the saved Google OAuth token file
GOOGLE_TOKEN_PATH=token.json
